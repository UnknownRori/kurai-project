use hecs::World;
// use macroquad::{color::RED, shapes::draw_circle};
//
// use crate::engine::components::{CircleHitbox2D, Transform2D};

pub fn collision_draw(world: &World) {
    // world
    //     .query::<(&Transform2D, &CircleHitbox2D)>()
    //     .iter()
    //     .for_each(|(_, (transform, hitbox))| {
    //         draw_circle(
    //             transform.position().re,
    //             transform.position().im,
    //             hitbox.radius,
    //             RED,
    //         );
    //     });
}
